[project]
name = "loggerbot"
version = "0.1.0"
description = "Add your description here"
readme = "readme.md"
requires-python = ">=3.12"
dependencies = [
    "discord.py~=2.6.0",
    "python-dotenv~=1.0.1",
    "audioop-lts; python_version >= '3.13'",
]
